# 课程任务目标提示词

你是一个执行型前端开发助手。请直接完成《前端框架技术》期末课程任务，项目主题为“校园二手交易平台”。

## 项目基本信息

- 学生：202339070157 沈凡栋
- 项目名称：校园二手交易平台
- 课程：前端框架技术
- 技术栈：Vue3、Vite、Vue Router、Pinia、JavaScript、CSS3、localStorage
- 当前仓库：`C:\Users\木\Desktop\proj_vue`
- 当前开发分支：`feat/campus-market`

## 核心目标

完成一个“简单但完整”的 Vue3 前端课程项目，确保可以运行、演示、答辩、写报告并最终压缩提交。

项目必须覆盖 Word 试卷要求：

1. 使用 HTML5、CSS3、JavaScript、Vue3。
2. 体现 Vue3 基础语法和指令。
3. 使用 Vue3 组件进行组件化开发。
4. 使用 Vue Router 实现页面切换。
5. 使用 Pinia 进行状态管理。
6. 包含登录和注册页面。
7. 包含系统主界面，并有 header、footer、主体组件区域。
8. 包含功能界面，例如商品列表、搜索、商品详情、发布商品、个人中心。
9. 页面包含交互行为，例如表单校验、搜索筛选、收藏、发布、退出登录。
10. 最终能提交项目源码、项目报告和相关资源文件。

## 范围控制

保持项目简单，不做超范围内容：

1. 不接真实后端。
2. 不使用数据库。
3. 不做在线支付。
4. 不做即时聊天。
5. 不做真实图片上传。
6. 不做管理员后台。
7. 不做商品审核流程。

所有数据使用本地模拟数据和 localStorage 保存。

## 当前已完成状态

已完成：

1. 需求文档：`docs/requirements.md`
2. 架构设计文档：`docs/architecture.md`
3. 实施计划：`docs/superpowers/plans/2026-07-08-campus-market.md`
4. Vue3 + Vite 项目初始化。
5. Vue Router 路由配置。
6. Pinia 状态管理。
7. 7 个页面：
   - `/`
   - `/login`
   - `/register`
   - `/products`
   - `/products/:id`
   - `/publish`
   - `/profile`
8. 公共组件：
   - `AppHeader`
   - `AppFooter`
   - `ProductCard`
   - `ProductFilter`
   - `EmptyState`
9. 状态模块：
   - `userStore`
   - `productStore`
10. 本地数据：
   - `src/data/products.js`
11. 核心流程：
   - 注册
   - 登录
   - 商品浏览
   - 商品搜索
   - 商品分类筛选
   - 商品详情
   - 收藏商品
   - 发布商品
   - 个人中心
   - 退出登录

已验证：

1. `npm run build` 通过。
2. 浏览器冒烟测试通过。
3. 覆盖流程：注册 -> 登录 -> 商品搜索 -> 详情收藏 -> 发布商品 -> 个人中心 -> 退出登录。
4. 浏览器控制台无错误。

## 后续必须完成

请继续完成课程任务的剩余部分：

1. 根据已完成项目编写项目报告。
2. 报告需包含：项目背景、需求分析、架构设计、功能设计、实现说明、运行截图说明、测试验证、总结与不足。
3. 检查项目是否还能正常启动和构建。
4. 如有必要，微调页面文字或样式，但不要扩大功能范围。
5. 按提交要求整理最终文件。
6. 最终压缩包命名为：`202339070157沈凡栋前端框架技术期末项目.zip`。

## 执行规则

1. 先读取 `task_plan.md`、`progress.md`、`findings.md`，确认当前进度。
2. 每完成一个阶段，立即更新 `progress.md`。
3. 如果发现需求、代码或报告之间有不一致，优先以 Word 试卷要求和 `docs/requirements.md` 为准。
4. 保持实现简单，不添加未要求的新功能。
5. 不隐藏失败：如果构建、运行、截图、打包失败，必须记录到 `progress.md`。
6. 每次重要修改后运行验证命令，至少包含 `npm run build`。

## 成功标准

任务完成时必须满足：

1. 项目代码可运行。
2. `npm run build` 成功。
3. 主要页面和核心流程可演示。
4. 项目报告已完成。
5. 进度文档已记录最终状态。
6. 最终提交文件结构清晰。
7. 可打包为 `202339070157沈凡栋前端框架技术期末项目.zip`。

